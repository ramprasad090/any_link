name: any_link_stream
description: "SSE (fixed for Flutter Web), WebSocket, LLM token streaming, and unified realtime client. Zero extra dependencies."
version: 1.0.0
homepage: https://github.com/ramprasads090/any_link
repository: https://github.com/ramprasad090/any_link
issue_tracker: https://github.com/ramprasad090/any_link/issues

topics:
  - sse
  - websocket
  - streaming
  - realtime
  - llm

platforms:
  android:
  ios:
  web:
  macos:
  windows:
  linux:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  any_link: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
